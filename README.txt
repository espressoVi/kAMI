kAMI software.
--------------------------------
To run first ensure all python dependencies are in order. Can be found in dependency.yml
Install with anaconda 
$ conda env create -f dependency.yml 
Then run with
$ python app.py
(do not close the terminal)
Open a browser and navigate to 
http://localhost:5000/
The web-app will be running. Enjoy!
